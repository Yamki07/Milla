<?xml version="1.0" encoding="utf-8"?>
<!-- Explicit content badge — subtle bordered rectangle -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/millay_badge_explicit_bg" />
    <corners android:radius="3dp" />
    <stroke
        android:width="1dp"
        android:color="@color/millay_badge_explicit" />
    <size
        android:width="16dp"
        android:height="16dp" />
</shape>
